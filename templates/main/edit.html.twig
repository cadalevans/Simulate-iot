{% extends 'style.html.twig' %}



{% block body %}


<div class="container mt-4">


    <h1>New Main Entry</h1>

    {{ form_start(form) }}
        {{ form_row(form.test) }}
        {{ form_row(form.date) }}

        <button type="submit" class="btn btn-primary">Save</button>
    {{ form_end(form) }}

</div>

{% endblock %}
